version: 1.0
runtime: python3
build:
  commands:
    build:
      - python --version
      - pip3 install -r requirements.txt
run:
  command: python server.py
  network:
    port: 8000
  env:
    - name: PORT
      value: "8000"
