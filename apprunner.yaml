version: 1.0
runtime: python311
build:
  commands:
    build:
      - python --version
      - python3 install -r requirements.txt
run:
  command: python server.py
  network:
    port: 8000
  env:
    - name: PORT
      value: "8000"
